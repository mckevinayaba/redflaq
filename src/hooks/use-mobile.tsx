import * as React from "react";

// RedFlaq is a mobile-first app. Every screen renders its mobile version,
// even on desktop (where it sits inside a centered phone frame via App.tsx).
export function useIsMobile() {
  return true;
}
